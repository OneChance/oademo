<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zh.oademo.oademo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".sign.SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".sign.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".mainframe.MainPageActivity"
            android:label=""
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".work.WorkitemActivity"
            android:label=""
            android:parentActivityName=".work.WorkListActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".work.WorkListActivity"
            android:label=""
            android:launchMode="singleTop"
            android:parentActivityName=".mainframe.MainPageActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".info.InfoListActivity"
            android:label=""
            android:launchMode="singleTop"
            android:parentActivityName=".mainframe.MainPageActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".info.InfoItemActivity"
            android:label=""
            android:parentActivityName=".info.InfoListActivity"
            android:screenOrientation="portrait" />

        <service
            android:name=".update.MyAccessibilityService"
            android:label="@string/accessibility_service_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>

</manifest>